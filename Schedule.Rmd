---
title: "Schedule"
output: html_document
---

All readings and assignemnts will be available through Blackboard.

```{r, cache=FALSE, warning=FALSE, message=FALSE, echo=FALSE, eval=TRUE}
library(readxl)
Lectures <- read_excel("schedule.xlsx", sheet = 1, col_names = TRUE, col_types = NULL, na = "", skip = 0)
Lectures[is.na(Lectures)]<- " "
#Lectures <- fread("Lectures.csv")
knitr::kable(Lectures)

```

